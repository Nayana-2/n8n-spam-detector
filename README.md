# n8n Spam Detector Workflow

This repository contains an n8n workflow configuration file that automatically checks incoming Gmail messages for spam using a local API endpoint.

## Project Structure
* `spam-detection.json`: The complete n8n workflow export.
